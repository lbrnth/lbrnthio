<!DOCTYPE html>

<html>

    <header>
        <link rel="stylesheet" href="/public/stylesheets/style.css">
        <title>{{title}}</title>
    </header>

    {{>header}}
        <p>{{errorMessage}}</p>
    {{>footer}}
</html>